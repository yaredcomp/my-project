name: Test Project
on:
  pull_request:
    branches:
      - main
jobs:
  test:
    runs-on: ubuntu-latest
    container:
      image: node:20
    steps:
      - uses: actions/checkout@v2
      # with:
      #  node-version: 20
      - run: npm ci
      - run: npm run build
# command: gitlens.graph.push?%7B%22webview%22%3A%22gitlens.views.graph%22%7D